# Server-only. Never inlined into the browser bundle.
# Optional lock for public preview URLs (dev/preview servers).
PREVIEW_PASSWORD=

# Used only by a future token-issuer backend. Do not put this in client code.
GEMINI_API_KEY=
